import { OrgDirectoryPage } from '@/features/enterprise-admin';

export const metadata = {
  title: 'Org Directory | Dezai Enterprise',
  description: 'Browse all employees with department and manager information.',
};

export default function Page() {
  return <OrgDirectoryPage />;
}
